# Runtime state: the "autostart is disabled for this boot" flag and the session
# failure counters live here, so they vanish on reboot by construction.
d /run/loom 0755 root root -
d /var/lib/loom 0755 root root -
