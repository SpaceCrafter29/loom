# Same kernel, initramfs built without autodetect: every module rather than the
# ones this machine needed last time. The entry to pick when new hardware, a
# moved disk or a botched upgrade means the normal image cannot find the root.
title      Loom (fallback)
sort-key   20-loom-fallback
efi        /EFI/loom/loom-linux-fallback.efi
