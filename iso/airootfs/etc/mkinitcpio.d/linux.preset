# mkinitcpio preset for the `linux` package inside the live medium.
# mkarchiso looks for exactly this preset name and builds the 'archiso' entry.
PRESETS=('archiso')

ALL_kver='/boot/vmlinuz-linux'
ALL_config='/etc/mkinitcpio.conf'

archiso_image="/boot/initramfs-linux.img"
